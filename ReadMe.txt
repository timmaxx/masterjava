main\resources
    cities.xsl - Содержит XSLT (XSL Transformation). Используется в XsltProcessorTest.
    payload.xsd - Содержит XML схему. Нужен для всех тестов.


test\resources
    city.xml - Одиночный элемент данных (без привязки к схеме). Используется только в JaxbParserTest.
    payload.xsd - Содержит данные (которые должны удовлетворять схеме). Нужен для всех тестов.


MatrixBenchmark размещён в папке src/main. В этом классе расположен код тестирования производительности.
Разве не логичней этот класс было-бы разместить в папке src/test?
Или даже вообще в какой-то другой папке, в которой даже не модульные тесты, а тесты производительности? Ну например src/perf?
Ведь и с точки зрения целей Maven, тест на производительность уместно делать после исполнения модульных тестов.
Вижу, что IDEA может маркировать папки (Mark Directory as) шестью вариантами или снять маркировку. Т.е. вроде как всего семь вариантов получается.
Возможно-ли ещё как-то маркировать папку? - В т.ч. и для хранения отдельно тестов производительности.


В видео 4_JAXB.mp4 (00:33) видно в контекстном меню пункт "WebServices". У меня его нет.
Поискал и установил "Jakarta EE: Web Services (JAX-WS) plugin". После этого через главное меню Tools - XML Actions появился Generate Java Code From Schema using JAXB.
Классы сгенерировались. Но в результате, в сгенерированных классах, импортировались классы jakarta.xml.bind... И приходилось везде исправить jakarta на javax.
Может есть какой-то другой плагин или нужно настроить этот плагин, что-бы этих исправлений не делать?